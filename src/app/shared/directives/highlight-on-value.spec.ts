import { HighlightOnValue } from './highlight-on-value';

describe('HighlightOnValue', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnValue();
    expect(directive).toBeTruthy();
  });
});
